# Logs

This directory contains runtime logs and intentionally retained logging examples. Local or transient logs should remain untracked; the repository `.gitignore` excludes common log artifacts by default.
