# Input data

Place small, intentionally versioned input fixtures here. Scripts should read from this directory rather than embedding large sample payloads in lesson files.

Generated files do not belong here; use `../outbound/` instead.
